# Pinned to the 1.56.x line. The codebase is FULLY off every
# deprecated Streamlit API — ZERO components.v1.html calls remain:
#   * components.v1.html  -> st.iframe (via src/components_compat.py)
#     for the 24 self-contained table/timeline iframes
#   * the one host-page JS injector (video-popup overlay) -> st.html
#     with unsafe_allow_javascript=True (not iframed, full host DOM)
#   * use_container_width= -> width='stretch'/'content' everywhere
# Requires Python >= 3.10 (see .python-version) — Streamlit dropped
# 3.9 after 1.50. Cap at <1.57 so the next minor can't silently land
# a new deprecation before we've validated against it.
streamlit>=1.56.0,<1.57
# Authlib >=1.6 changed how OAuth state is stored in the session, which is
# incompatible with Streamlit's OIDC handler in 1.57 and earlier — it surfaces
# as MismatchingStateError on /oauth2callback after a Cloud reboot resolves
# the latest Authlib. Pin to the last known-working line.
Authlib>=1.3.2,<1.6.0
google-api-python-client>=2.100.0
supabase>=2.0.0
plotly>=5.18.0
pandas>=2.1.0
python-dotenv>=1.0.0
isodate>=0.6.1
anthropic>=0.30.0
streamlit-cookies-controller>=0.0.4
# Language detection from video titles (Season-page language mix charts).
# Pure-Python, MIT, no compiled deps.
lingua-language-detector>=2.0.0
