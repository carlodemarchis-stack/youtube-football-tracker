# Upper bound is load-bearing: Streamlit deprecated st.components.v1.html
# (removed in the release AFTER 2026-06-01) and the use_container_width=
# kwarg. ~25 call sites render raw HTML in sandboxed iframes WITH embedded
# <script> (sortable .ac-table/.wc-tbl tables, timelines) — st.html()
# does not execute scripts and st.iframe (the named replacement) does not
# exist until a post-1.50 release, so it cannot be migrated against the
# current API. Unpinned, the first post-removal release would auto-install
# on the next Railway redeploy and hard-break those tables. Stay on the
# 1.50.x line (patch fixes OK) until the components.html -> st.iframe +
# width= migration is done deliberately on a chosen newer version.
streamlit>=1.42.0,<1.51
# Authlib >=1.6 changed how OAuth state is stored in the session, which is
# incompatible with Streamlit's OIDC handler in 1.57 and earlier — it surfaces
# as MismatchingStateError on /oauth2callback after a Cloud reboot resolves
# the latest Authlib. Pin to the last known-working line.
Authlib>=1.3.2,<1.6.0
google-api-python-client>=2.100.0
supabase>=2.0.0
plotly>=5.18.0
pandas>=2.1.0
python-dotenv>=1.0.0
isodate>=0.6.1
anthropic>=0.30.0
streamlit-cookies-controller>=0.0.4
# Language detection from video titles (Season-page language mix charts).
# Pure-Python, MIT, no compiled deps.
lingua-language-detector>=2.0.0
