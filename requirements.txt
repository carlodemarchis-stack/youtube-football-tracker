# Pinned to the 1.56.x line: 1.56.0 introduced st.iframe (the named
# replacement for the deprecated st.components.v1.html, removed after
# 2026-06-01) and the width='stretch'/'content' API (replacing
# use_container_width=). The codebase is fully migrated to both
# (src/components_compat.py shims components.v1 -> st.iframe; all
# use_container_width= are now width=). Requires Python >= 3.10
# (see .python-version) — Streamlit dropped 3.9 after 1.50. Cap at
# <1.57 so the next minor can't silently land a new deprecation.
streamlit>=1.56.0,<1.57
# Authlib >=1.6 changed how OAuth state is stored in the session, which is
# incompatible with Streamlit's OIDC handler in 1.57 and earlier — it surfaces
# as MismatchingStateError on /oauth2callback after a Cloud reboot resolves
# the latest Authlib. Pin to the last known-working line.
Authlib>=1.3.2,<1.6.0
google-api-python-client>=2.100.0
supabase>=2.0.0
plotly>=5.18.0
pandas>=2.1.0
python-dotenv>=1.0.0
isodate>=0.6.1
anthropic>=0.30.0
streamlit-cookies-controller>=0.0.4
# Language detection from video titles (Season-page language mix charts).
# Pure-Python, MIT, no compiled deps.
lingua-language-detector>=2.0.0
